# Development

This document covers local setup, application environment variables, local authentication email testing, project structure, and available development commands.

# Prerequisites

Install:

- Node.js LTS
- npm
- Supabase CLI
- Docker

# Quick local setup

The easiest way to start the complete environment is:

```bash
npm run demo:start
```

This:

1. Installs dependencies.
2. Starts local Supabase services.
3. Applies database migrations and seed data.
4. Generates `.env.local`.
5. Starts the Next.js development server.

The application runs at:

```text
http://localhost:3000
```

The generated local environment enables the outer access gate:

```text
ACCESS_GATE_DISABLED=false
```

Create an invite after resetting the local database:

```bash
npm run invite:create -- --label "Local demo" --days 14
```

To bypass the outer invite gate during local development, set:

```text
ACCESS_GATE_DISABLED=true
```

in `.env.local` and restart the development server.

This bypass is ignored in Azure Container Apps.

Setting `ACCESS_GATE_DISABLED=true` therefore cannot disable the deployed production gate.

# Manual local setup

Install dependencies:

```bash
npm install
```

Start local Supabase services:

```bash
npm run infra:up
```

Reset the database and apply migrations and seed data:

```bash
npm run infra:reset
```

Generate local environment configuration:

```bash
npm run infra:env
```

Create an invite:

```bash
npm run invite:create -- --label "Local demo" --days 14
```

Start the Next.js development server:

```bash
npm run dev
```

After another:

```bash
npm run infra:reset
```

recreate local invites because the local database has been reset.

# Local email testing

Authentication emails are captured by MailPit instead of being delivered externally.

Open MailPit at:

```text
http://localhost:54324
```

## Sign-up confirmation

1. Pass the invite gate or disable it locally.
2. Open `/auth/sign-up`.
3. Register an account.
4. Open MailPit.
5. Select the confirmation email.
6. Follow the confirmation link.

## Password recovery

1. Open `/auth/forgot-password`.
2. Submit the account email address.
3. Open MailPit.
4. Select the password-reset email.
5. Follow the reset link.
6. Choose a new password.

MailPit keeps local authentication testing deterministic and avoids requiring an external email provider.

# Environment variables

## Application runtime

The application uses:

```text
NEXT_SUPABASE_URL
NEXT_SUPABASE_PUBLISHABLE_KEY
ACCESS_GATE_CODE_SECRET
ACCESS_GATE_COOKIE_SECRET
```

The two access-gate secrets:

- must be at least 32 characters;
- should be different values.

Local development may additionally use:

```text
ACCESS_GATE_DISABLED
```

Setting this to:

```text
true
```

bypasses the invite gate only outside Azure.

Local values can be generated with:

```bash
npm run infra:env
```

## Invite operator tooling

Creating invites additionally requires one trusted Supabase administrative credential:

```text
SUPABASE_SECRET_KEY
```

or the legacy fallback:

```text
SUPABASE_SERVICE_ROLE_KEY
```

These values are only for trusted operator tooling.

They must not be exposed to browser code.

## Database types

Supabase-generated database types are the source of truth for database-backed TypeScript structures.

Application types such as:

- consultation rows;
- consultation status;
- application roles

derive from the generated database schema rather than manually duplicating PostgreSQL definitions.

After changing database schema migrations, regenerate the types with:

```bash
npm run db:types
```

Generated output lives at:

```text
lib/supabase/database.types.ts
```

Do not edit this file manually.

Valibot runtime validation remains separate from generated TypeScript types because compile-time types cannot establish that external runtime data matches the expected contract.

# Available scripts

| Command                 | Purpose                                                |
| ----------------------- | ------------------------------------------------------ |
| `npm run dev`           | Start the Next.js development server                   |
| `npm run build`         | Create a production build                              |
| `npm run start`         | Start the production server                            |
| `npm run typecheck`     | Run TypeScript without emitting files                  |
| `npm run lint`          | Run ESLint                                             |
| `npm run format`        | Format Markdown, TypeScript, and SQL                   |
| `npm run format:check`  | Check formatting                                       |
| `npm test`              | Run all Vitest projects                                |
| `npm run test:node`     | Run the Node Vitest project                            |
| `npm run test:ui`       | Run browser component tests headlessly in Chromium     |
| `npm run test:watch`    | Run Node tests in watch mode                           |
| `npm run test:ui:watch` | Run browser tests interactively                        |
| `npm run test:db`       | Run PostgreSQL RLS, validation, and access-gate checks |
| `npm run infra:up`      | Start local Supabase services                          |
| `npm run infra:reset`   | Reset the database and apply migrations and seed data  |
| `npm run infra:env`     | Generate local Supabase and gate environment variables |
| `npm run infra:down`    | Stop local Supabase services                           |
| `npm run invite:create` | Create an invite whose lifetime begins on first use    |
| `npm run demo:start`    | Prepare and start the complete local demonstration     |
| `npm run db:types`      | Regenerate TypeScript database types                   |

`npm run format` runs Prettier and then `pg_format` for SQL files. The
formatter check currently covers the files supported by Prettier; use
`npm run format:sql` when you need to apply SQL formatting directly.

Database migrations are the executable source of truth. After changing a
migration locally, run `npm run infra:reset` before `npm run test:db` so the
running Supabase instance applies the new migration.

# Project structure

```text
app/
├── api/
│   ├── access/
│   │   └── unlock/
│   │       ├── helpers.ts
│   │       ├── route.ts
│   │       └── types.ts
│   ├── admin/
│   │   └── consultations/
│   │       └── route.ts
│   ├── consultations/
│   │   ├── [id]/
│   │   │   ├── helpers.ts
│   │   │   └── route.ts
│   │   └── route.ts
│   └── health/
│       └── route.ts
├── auth/
├── protected/
├── page.tsx
└── layout.tsx

components/
├── admin/
├── student/
│   ├── consultation-item.tsx
│   ├── consultation-list-student.tsx
│   ├── create-consultation-card.tsx
│   ├── student-consultation-action-hook.ts
│   ├── student-consultation-hook.ts
│   └── student-consultations-view.tsx
└── ui/
    └── forms/
        └── access-gate-form.tsx

lib/
├── access-gate/
│   ├── constants.ts
│   ├── cookie.ts
│   ├── env.ts
│   ├── hash.ts
│   ├── paths.ts
│   ├── proxy.ts
│   ├── session-cache.ts
│   └── session-validation.ts
├── consultations/
│   ├── api.ts
│   └── schemas.ts
├── rate-limiter/
│   ├── client.ts
│   ├── in-memory.ts
│   └── types.ts
├── server/
│   └── auth.ts
├── supabase/
│   ├── database.types.ts
│   ├── proxy.ts
│   └── server.ts
└── validation/
    ├── schemas/
    ├── types/
    └── validate.ts

test/
├── app/
├── components/
│   ├── auth/
│   ├── student/
│   └── ui/
├── fixtures/
├── lib/
│   ├── access-gate/
│   ├── consultations/
│   ├── rate-limiter/
│   ├── server/
│   └── supabase/
└── scripts/

scripts/
├── container-tests/
│   ├── docker.mjs
│   ├── http.mjs
│   ├── run-container-tests.mjs
│   └── supabase.mjs
├── create-access-invite.mjs
├── generate-local-env.mjs
└── run-rls-tests.mjs

supabase/
├── migrations/
├── tests/
│   ├── access_gate_checks.sql
│   └── rls_checks.sql
├── config.toml
├── schema.sql
└── seed.sql

docker/
└── Dockerfile

deploy/
└── azure/
    ├── bootstrap-oidc.sh
    └── main.bicep

docs/
├── images/
├── access-control.md
├── api.md
├── architecture.md
├── deployment.md
├── development.md
├── secrets-management.md
└── testing.md

.github/
└── workflows/
    ├── ci.yml
    ├── container-stage.yml
    ├── dependency-review.yml
    ├── production.yml
    ├── production-teardown.yml
    └── take-containers-offline.yml

proxy.ts
```

# Key responsibilities

- `app/page.tsx` — public invite-gate entry screen
- `app/api/access/unlock/**` — rate-limited invite validation and access-cookie issuance
- `lib/access-gate/**` — invite hashing, cookie signing, safe redirects, environment validation, RPC validation, session caching, and proxy gate logic
- `lib/rate-limiter/**` — client identification and bounded in-memory rate limiting
- `proxy.ts` — access-gate and Supabase session orchestration
- `app/auth/**` — authentication screens, Server Actions, and callback routes
- `app/protected/page.tsx` — authenticated role-aware dashboard entry
- `components/student/student-consultation-hook.ts` — SWR-backed student query state
- `components/student/student-consultation-action-hook.ts` — mutation orchestration and cache revalidation
- `components/student/consultation-item.tsx` — row presentation and row-local interaction state
- `components/admin/**` — read-only administrator interface
- `lib/consultations/api.ts` — HTTP client, error handling, and runtime response validation
- `lib/consultations/schemas.ts` — consultation response schemas
- `lib/validation/**` — runtime application validation
- `app/api/consultations/**` — student consultation operations and ownership enforcement
- `app/api/admin/consultations/**` — administrator read-only API
- `lib/server/auth.ts` — authenticated application context and role authorization
- `lib/supabase/database.types.ts` — generated Supabase/PostgreSQL types
- `lib/supabase/**` — typed Supabase clients and SSR session handling
- `supabase/migrations/**` — executable database schema history
- `supabase/tests/**` — RLS and invite-gate database checks
- `scripts/create-access-invite.mjs` — trusted invite operator tool
- `scripts/container-tests/**` — production-container integration harness
- `docker/Dockerfile` — production standalone Next.js container
- `deploy/azure/**` — Azure Container Apps infrastructure and OIDC bootstrap
- `.github/workflows/**` — CI, staging, dependency review, deployment, and production operations

# HTTP API overview

The complete endpoint contract is documented in [API reference](api.md).

All application API routes are protected by the outer access gate unless they
are explicit operational or callback exceptions. Consultation routes then add
Supabase authentication, role, ownership, and RLS checks of their own.
